packages:
  # Completely ignoring lower-precedence configuration options is supported with the :: notation for keys
  mpi::
    require: mpich
  perl:
    externals:
    - spec: perl@5.26.1
      prefix: /usr
    buildable: false
  libtool:
    version: [:2.4.6]
  openmpi:
    version: [4.1.1:]
