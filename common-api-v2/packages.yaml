packages:
  # Completely ignoring lower-precedence configuration options is supported with the :: notation for keys
  mpi::
    require: openmpi
  perl:
    externals:
    - spec: perl@5.26.3
      prefix: /usr
    buildable: false
  libtool:
    version: [:2.4.6]
  all:
    target: [x86_64]
