packages:
  # Completely ignoring lower-precedence configuration options is supported with the :: notation for keys
  mpi::
    require: openmpi
  perl:
    externals:
    - spec: perl@5.26.3
      prefix: /usr
    buildable: false
  all:
    target: [x86_64_v3]
